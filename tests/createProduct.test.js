import requestLib from 'supertest';
import mongoose from 'mongoose';
import chai, { expect } from 'chai';

const url = 'http://localhost:3000/graphql';
const request = requestLib(url);

describe('createProduct', () => {});
